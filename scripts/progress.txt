# Ralph Progress Log
Started: Thu Jun 18 08:35:56 UTC 2026
---

## Codebase Patterns
- Stack: Next.js 15 (App Router, TS), React 19, Prisma+SQLite, NextAuth v4 + @auth/prisma-adapter (DB session strategy).
- Admin enforcement: pages -> `requireAdmin(callbackUrl)` from `@/lib/session` (redirects non-admins to `/forbidden`); API routes -> `requireAdminApi()` from `@/lib/api-auth` (returns `{error: NextResponse}` 401/403, else `{session}`). Hide admin UI via `session.user.role === "Admin"`.
- First-user-becomes-Admin: handled in `events.createUser` in `src/lib/auth.ts` (NOT a callback). Real OAuth sign-in triggers it.
- DB lives at `prisma/dev.db` (DATABASE_URL `file:./dev.db` is resolved relative to the schema dir).
- Run node/prisma scripts from the PROJECT ROOT so `@prisma/client` resolves (not from /tmp).
- Verify role-gated routes without real OAuth: insert a `User` + `Session` (sessionToken) row, then send cookie `next-auth.session-token=<token>` to the live server.
- Import the singleton `prisma` from `@/lib/prisma`; never instantiate PrismaClient directly.
- Auth: `authOptions` in `src/lib/auth.ts`. Providers added conditionally on env vars (missing creds => provider omitted, no crash).
- Session is DATABASE strategy. Attach `id`/`role` in the `session({session,user})` callback. Session type augmented in `src/types/next-auth.d.ts`.
- Protected routes: add prefix to BOTH `middleware.ts` (cookie-presence redirect to `/signin?callbackUrl=`) AND call `requireSession(callbackUrl)` from `@/lib/session` in the server component for real enforcement.
- Next 15 server components: `await searchParams` / `await params` (they are Promises).
- Run prisma/build/dev with env loaded: `set -a && . ./.env.local && set +a`.
- Migrations committed under `prisma/migrations/`; `dev.db` is gitignored.
- Browser verify with Playwright run from project root; chromium `--no-sandbox` at /home/agent/.cache/ms-playwright/chromium-1208/chrome-linux/chrome.

## 2026-06-18 08:45 UTC - US-001 OAuth sign-in
- Scaffolded the entire project foundation (greenfield: only package.json existed).
- Implemented OAuth sign-in with Google + Azure AD via NextAuth v4 + Prisma adapter.
- Files added: package.json, tsconfig.json, next.config.ts, .eslintrc.json, .gitignore,
  middleware.ts, prisma/schema.prisma (+ initial migration), src/lib/{prisma,auth,session}.ts,
  src/types/next-auth.d.ts, src/app/{layout,page,providers,globals.css},
  src/app/api/auth/[...nextauth]/route.ts, src/app/signin/{page,SignInButtons}.tsx,
  src/app/dashboard/page.tsx, AGENTS.md.
- Auth models: User(role default Reader), Account, Session, VerificationToken with cascade deletes.
- User email/name/avatar(image) stored from provider profile via adapter.
- Unauthed access to /dashboard redirects to /signin?callbackUrl=%2Fdashboard (middleware) — verified.
- Quality: typecheck PASS, lint PASS (0 warnings), build PASS. Browser verified sign-in page
  renders both provider buttons (screenshot) and redirect flow works.
- **Learnings for future iterations:**
  - @auth/prisma-adapter is compatible with next-auth v4.24+; cast adapter `as Adapter` to satisfy types.
  - Database session strategy means `next-auth/middleware` withAuth (JWT-based) won't work — use cookie-presence check in middleware + server-side getServerSession for enforcement.
  - `next lint` warns it's deprecated in Next 16 but still works; keep using `npm run lint` for now.
  - First-user-becomes-Admin (US-002) NOT yet done; all new users currently default to Reader.
  - Schema currently holds only auth models; US-031 will add Article/Progress/Vocabulary/etc.
---

## 2026-06-18 08:55 UTC - US-002 Role-based access control
- first-user-becomes-Admin via NextAuth `events.createUser` in src/lib/auth.ts.
- Added `requireAdmin(callbackUrl)` to src/lib/session.ts (non-admins -> /forbidden).
- Added src/lib/api-auth.ts: `requireSessionApi` (401) + `requireAdminApi` (403) helpers.
- Added /admin page (requireAdmin), /forbidden page, and /api/admin/stats route (admin-gated).
- Admin link on /dashboard shown only when role === Admin (UI hidden for Readers).
- middleware.ts: added /admin to PROTECTED_PREFIXES + matcher.
- Files: src/lib/auth.ts, src/lib/session.ts, src/lib/api-auth.ts (new),
  src/app/admin/page.tsx (new), src/app/forbidden/page.tsx (new),
  src/app/api/admin/stats/route.ts (new), src/app/dashboard/page.tsx, middleware.ts, AGENTS.md.
- Quality: typecheck PASS, lint PASS (0 warnings).
- Verification (8/8 via live server + seeded sessions): unauth /admin->/signin?callbackUrl;
  unauth API 401; Reader /admin->/forbidden; Reader API 403; Admin /admin 200 w/ content;
  Admin API 200; first user->Admin; second user->Reader.
- **Learnings for future iterations:**
  - first-user-Admin MUST live in `events.createUser`, not the `session`/`signIn` callbacks
    (adapter creates the row with default Reader; the event promotes it post-insert).
  - Playwright chromium NOT at the path in AGENTS.md ($HOME is /home/azadmin, not /home/agent);
    browser-binary probing was blocked. Used HTTP-level checks against the live `next dev`
    server with seeded session cookies instead — works well for role-gated route verification.
  - When adding a new admin route, gate BOTH the page (requireAdmin) and any API (requireAdminApi),
    and add the prefix to middleware (cookie presence only; real role check is server-side).
---

## 2026-06-18 09:05 UTC - US-003 Sign-out and session persistence
- Added reusable `src/components/SignOutButton.tsx` (client component; next-auth `signOut({callbackUrl})`).
- Surfaced a visible Sign out action on /dashboard and on the home page (when signed in).
- Made session persistence explicit in `src/lib/auth.ts`: session `maxAge` 30d, `updateAge` 24h
  (DB strategy already persisted across reloads; this sets the expiry window).
- Files changed: src/components/SignOutButton.tsx (new), src/app/dashboard/page.tsx,
  src/app/page.tsx, src/lib/auth.ts.
- Quality: typecheck PASS, lint PASS (0 warnings).
- Verification (live server + seeded Reader session cookie):
  - Dashboard renders "Sign out"; home shows "Signed in as ..." with session.
  - POST /api/auth/signout (csrf) -> 200, set-cookie clears next-auth.session-token (Max-Age=0),
    AND the Session row is DELETED from the DB (database strategy server-side termination).
  - After sign-out, /dashboard -> 307 redirect to /signin?callbackUrl=%2Fdashboard;
    home shows public "not signed in" state.
- **Learnings for future iterations:**
  - With DB session strategy, next-auth sign-out deletes the Session row server-side (not just the
    cookie) — verify both cookie clearing and DB row deletion.
  - signOut/signIn must be called from a "use client" component; keep them in src/components for reuse.
  - HTTP sign-out test: GET /api/auth/csrf (save cookie jar) then POST form-encoded csrfToken +
    callbackUrl + json=true to /api/auth/signout with the session cookie.
---

## 2026-06-18 09:15 UTC - US-004 First-run onboarding flow
- Added 1-1 `Profile` model (ageRange?, gender?, englishLevel, topics JSON string, completedAt)
  + migration `add_profile`; `profile Profile?` relation on User.
- New shared `src/lib/categories.ts` (CATEGORIES/CATEGORY_SLUGS/isValidCategorySlug) — reusable
  for US-017/US-018. `src/lib/profile.ts` (getProfile/isOnboarded/isUserOnboarded/parseTopics +
  AGE_RANGES/GENDERS/ENGLISH_LEVELS constants).
- `/onboarding` server page + `OnboardingForm.tsx` client component: English level (required,
  A1-C2), age range (optional), gender (optional), topic multi-select from category set.
- POST `/api/onboarding`: requireSessionApi, validates inputs, filters topics to valid slugs,
  upserts Profile with completedAt (marks complete).
- Gating: added `requireOnboardedSession()` to src/lib/session.ts; dashboard now uses it so
  un-onboarded users -> /onboarding. Onboarding page redirects already-completed users -> /dashboard.
- Files: prisma/schema.prisma (+migration), src/lib/categories.ts (new), src/lib/profile.ts (new),
  src/lib/session.ts, src/app/onboarding/{page,OnboardingForm}.tsx (new),
  src/app/api/onboarding/route.ts (new), src/app/dashboard/page.tsx, AGENTS.md.
- Quality: typecheck PASS, lint PASS (0 warnings).
- Verification (live server, seeded Reader session): new user /dashboard -> 307 /onboarding;
  /onboarding renders form; POST stores profile (bogus topic filtered out); completed user
  /onboarding -> 307 /dashboard and /dashboard -> 200. Playwright (chromium --no-sandbox, run
  from project root) drove the full UI: selected B2, toggled Tech/World, Finish setup -> /dashboard.
- **Learnings for future iterations:**
  - SQLite/Prisma has NO scalar list type — store arrays (topics) as JSON strings; use parseTopics.
  - Reuse `src/lib/categories.ts` for all category/topic UIs (don't redefine the 9-category set).
  - To gate a page on a finished profile use `requireOnboardedSession`; never call it from the
    onboarding page itself (infinite redirect) — that page uses requireSession + manual redirect.
  - Playwright DOES work here when the script runs from the project root (so @playwright/test
    resolves) with chromium launched `--no-sandbox`; direct binary-path probing is still blocked.
---

## 2026-06-18 09:00 UTC - US-005 Edit profile & account settings
- New `/settings` page (server, requireOnboardedSession): shows account info
  (name, email, avatar, role) + client `ProfileSettingsForm` to edit English level,
  topics, age range, gender. Save -> PUT /api/profile, then router.refresh().
- New `PUT /api/profile` route: validates + upserts profile but PRESERVES completedAt
  (so editing settings never re-triggers onboarding state).
- Refactored: extracted shared `parseProfileInput(body)` into src/lib/profile.ts
  (returns {ok,value}|{ok,error}); both /api/onboarding (sets completedAt) and
  /api/profile now use it. No behavior change to onboarding.
- Added a Settings link on /dashboard.
- Files: src/lib/profile.ts, src/app/api/onboarding/route.ts (refactor),
  src/app/api/profile/route.ts (new), src/app/settings/page.tsx (new),
  src/app/settings/ProfileSettingsForm.tsx (new), src/app/dashboard/page.tsx, AGENTS.md.
- Quality: typecheck PASS, lint PASS (0 warnings).
- Verification (live server + seeded onboarded Reader session cookie):
  - GET /settings 200 renders name/email/Role:Reader, preselected level B1, age 25-34,
    gender Female, highlighted topics Science+Tech (aria-pressed=true).
  - Unauth GET /settings -> 307 /signin?callbackUrl=%2Fsettings.
  - PUT /api/profile (B1->C1, topics business/health/bogus) -> 200; DB confirms
    englishLevel=C1, ageRange=35-44, gender=Male, topics=["business","health"]
    (bogus slug filtered), completedAt still present.
  - PUT bad level -> 400; unauth PUT -> 401.
- **Learnings for future iterations:**
  - Reuse `parseProfileInput` for any future profile-related API; don't re-validate inline.
  - Settings vs onboarding differ ONLY in completedAt handling: onboarding upsert sets it,
    settings upsert omits it on update (create still seeds it for safety).
  - Browser binary probing still blocked ($HOME=/home/azadmin); used HTTP-level rendering
    + DB assertions against `next dev` with a seeded session cookie. The rendered <select>
    emits `value="B1" selected` and topic buttons emit `aria-pressed="true"` — grep those
    to assert preselected form state without a real browser.
---
