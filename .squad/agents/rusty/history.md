# Project Context

- **Owner:** Yingting Huang
- **Project:** ReadWise — AI-assisted English learning reader. Articles are scraped from the internet; goal is a redesign into a modern, attractive, feature-rich website.
- **Stack:** Next.js 15 (App Router, TypeScript), React 19, Prisma + SQLite, NextAuth v4 (database sessions), Azure OpenAI / Azure Speech, Playwright.
- **Created:** 2026-06-19

## Learnings

<!-- Append new learnings below. Each entry is something lasting about the project. -->

### M2 — Global App Shell (2026-06-19) ✅ SHIPPED — committed 385de06
Route group `src/app/(app)/` pattern confirmed: URL-transparent, zero middleware churn, single layout ownership. Admin untouched (own layout). Root layout untouched (fonts + no-flash + Providers). Nit N1 resolved: Admin link belongs in UserMenu/MobileDrawer (not AppNav) per Saul's finalized spec.

### Redesign continuation session (2026-06-19T11:32:57+08:00)
Spawned by Scribe: produce redesign roadmap proposal (product-page migration to design system + curated rich-features menu) at `.squad/decisions/inbox/rusty-redesign-roadmap.md`. Context: M3 built by Linus, awaiting Basher verify; M4–M8 outstanding.

### Accepted Roadmap M4–M9 (2026-06-19) — ACCEPTED by Yingting
Roadmap proposal accepted. Key decisions recorded: M4–M9 (renamed/expanded from M4–M8; added new M7 "Onboarding, Auth & Settings polish"); migration-first sequencing; additive-migrations-only rule for all net-new features. Roadmap supersedes earlier M4–M8 sketch in decisions.md. Rich-features menu archived with the proposal.

### M4 — Code Review (2026-06-19) ✅ APPROVE-WITH-NITS — committed 7e554c9
Reviewed M4 working tree (linus-m4-built.md + livingston-m4-search.md). APPROVE-WITH-NITS verdict — no blockers:
- All five must-not-break constraints passed (ListingProgressSync hooks, no raw hex, no nested anchors, Prisma schema unchanged, US-030 cache tags unaffected).
- **NIR-1:** `listInProgressArticles` published filter runs JS-side after `take`; should be in Prisma `where` (take may under-fill rail when unpublished rows exist).
- **NIR-2:** `--experimental-strip-types` absent from CLI scripts (`npm run scrape|process|worker|seed`) — works today but asymmetric with test script.
- **NIR-3:** Duplicate `role="region"` landmark on continue-reading rail (`<section aria-label>` + inner `<div role="region">`).
- **NIR-4:** Two separate `@/lib/cn` imports in `CategoryBrowser.tsx`.
Linus nit-cleanup in progress.

### M5 — Reader Redesign Code Review (2026-06-19) ✅ APPROVE-WITH-NITS — committed f199596
Reviewed M5 working tree (linus-m5-built.md). APPROVE-WITH-NITS verdict — no blockers. All six review scopes passed: audio context architecture, tabbed panel mount-preservation, AI panel refactors, reader-prefs + no-flash, reading-mode scope, must-not-break. Key findings: `useCallback([words])` is stale-closure-free ✅; `hidden` attribute panel lifecycle correct ✅; `data-reading-mode` correctly reader-scoped, never touches `<html>` ✅; `sanitizeArticleHtml`→`WordLookup` sole HTML path ✅. Four nits: NIR-M5-1 (double mobile PanelContents → M6), NIR-M5-2 (firstFocusRef unassigned, focus-move missing → M9), NIR-M5-3 (missing `<main>` landmark — fix-before-land, FIXED by Linus), NIR-M5-4 (dead isMounted ref — FIXED by Linus). M5 LANDED f199596.

### M6 — Dashboard & Study Gamification Code Review (2026-06-19) ✅ APPROVE-WITH-NITS — committed 1beea38
Reviewed M6 working tree. APPROVE-WITH-NITS verdict — no blockers. All 8 review scopes passed: migration (purely additive), SM-2 engine (EF floor, grade semantics, no NaN/negative intervals), activity/streak (UTC midnight anchor, idempotency, longestStreak scan correct), flashcard ownership guard (loads card → userId check → ApiError 404), tests (meaningful coverage of all grades, streak anchors, ownership paths), UI wiring (appStateRef stale-closure guard, double-grade prevention, server-side `getStreakSummary` in Promise.all), accent rule + a11y (teal only on reading-state, indigo on interactive, aria-live, keyboard, reduced-motion), regression safety (M4/M5 untouched). Two FIX-BEFORE-LAND: F1 srs.ts doc-comment (Livingston) — 1.2× → 0.6× correction; F2 hoverStyle dead code (Linus) — destructure + apply inline style. Seven deferrable nits (D1–D5 carry to M7, N4 → M9 a11y, N5 trivial type widening). Both fixes applied and verified; M6 LANDED 1beea38.

### M7 — Onboarding / Auth / Settings Code Review (2026-06-19) ✅ APPROVE — committed cb204c5
Reviewed M7 working tree (Livingston backend + Linus UI). Clean **APPROVE** — no blockers, no FIX-BEFORE-LAND items. All 13 review scopes passed: auth flow (`signIn`/`callbackUrl` guard/`?error=` mapping unchanged), onboarding data parity (POST body byte-identical; `englishLevel` gate double-guarded; `completedAt` server-side), gating unchanged (middleware untouched; `requireSession`/`requireOnboardedSession` unchanged), `parseProfileInput` + daily-goal (constants exported, hard-reject, omit-preserves, conditional spread, 9 new tests), settings wiring (page reads `??DAILY_GOAL_DEFAULT`; form sends `dailyGoal`; stepper clamps correctly), M1 primitives/tokens (no raw colors introduced), accent rule, a11y focus management, a11y radio-cards, a11y chips, a11y stepper progress, a11y heading order, a11y daily-goal stepper, a11y error/success, `globals.css` additive, reduced-motion, M4/M5/M6 untouched, no schema/migration changes. Four deferrable nits → M9/M8: N1 `onBlur` clamp, N2 stepper pill semantics, N3 `CardTitle` level prop, N4 `LEVEL_HINTS` duplication. M7 LANDED cb204c5.

### M8 — Admin Design System Polish Code Review (2026-06-19) ✅ APPROVE — committed a631aa9
Clean **APPROVE** — no FIX-BEFORE-LAND items. All 7 scopes passed: no behavior/gating/API/schema drift (`requireAdmin` + all sub-page gates intact; destructive action payloads byte-identical); ConfirmAction a11y (`role="alertdialog"`+`aria-label`+cancel-first focus+Escape+`aria-expanded`, all 3 callers correct, member self-protection preserved); Playwright selectors preserved via ConfirmAction anatomy; accent rule (indigo pill nav, bar fill via `--primary`, zero new raw hex); `CardTitle level` non-breaking; globals.css retired classes commented not deleted; diff scope strictly admin area + 2 primitive files. Four deferrable nits → M9: N1 dual-open panels (AdminArticleActions), N2 `statusBadgeVariant` copied 3×, N3 alertdialog missing `aria-describedby`, N4 `min-width:220px` dropped from `.admin-actions`.
### M9 — Command Palette + Final A11y/Motion QA Code Review (2026-06-19) ✅ APPROVE-WITH-NITS — committed dff6c1f
Reviewed M9 Pass A (palette) + Pass B (15-nit sweep). **APPROVE-WITH-NITS** — one FIX-BEFORE-LAND applied before land. All 14 Pass-A review areas + 14 Pass-B nits verified. Pass-A highlights: ARIA combobox/listbox pattern correct (focus stays on input, `aria-activedescendant` drives highlight); keyboard model complete (↑↓/Home/End/Enter/Esc/Tab trap/"/" guard); stale-closure safety via `selectableItemsRef`/`activeIndexRef` + `latestQueryRef`; accent rule clean (active row indigo, zero errant teal); reduced-motion identity transforms (not duration-0). FIX-BEFORE-LAND: `aria-expanded={true}` on combobox (was `selectableItems.length > 0` — ARIA 1.2 violation when "No results" shown; applied by Linus). Two deferrable post-M9 nits: NIT-1 `GoalMetIcon` reactive animation dead letter (SSR flash fixed, reactive not-met→met never fires — not a regression); NIT-2 `loadMore` missing stale guard (applied as FIX-2 pre-land). All must-not-break contracts untouched. **Completes redesign roadmap M4–M9.**

### M10 — Bookmarks & Reading Lists Code Review (2026-06-19) ✅ APPROVE-WITH-NITS — committed c676921
Reviewed M10 for IDOR, security, ListingProgressSync contract integrity, a11y, and code quality. **APPROVE-WITH-NITS** — no blockers, no IDOR. IDOR deep-dive: all 9 routes verified — every helper uses `findFirst({where:{id,userId}})` before mutation; 404 (not 403) on ownership failure; `/lists` page resolves `listParam` only within userId-filtered results; `getListWithArticles` adds second ownership layer. **No IDOR path exists.** `/api/saved` batch endpoint: `createHandler` session-gated, `array(nonEmptyString(200),{max:200})` validated, user-scoped — PASS. Migration purely additive, cascades correct. Sibling-overlay pattern correct: all 5 `.js-progress-*` hooks + `data-article-id` on `<Link>` verbatim unchanged; `ListingBookmarkSync` separate file, never touches progress hooks. Accent rule upheld: `var(--primary)` (indigo) throughout, zero teal in bookmark UI. 6 deferrable nits (N1 default-list DB uniqueness race, N2 TOCTOU rename/delete, N3 `role="tablist"` on `<Link>` a11y, N4 dual-DOM aria-hidden, N5 load-more cards `saved=false`, N6 `ConfirmAction !p-0`). 191/191 tests pass, 0 regressions to M4–M9.

### M11 — Highlights & Notes Code Review (2026-06-19) ✅ APPROVE-WITH-NITS — committed 1e69c01
Reviewed M11 (Linus UI + Livingston backend). **APPROVE-WITH-NITS** — no sanitize bypass, no XSS, no IDOR. Sanitize guarantee: `dangerouslySetInnerHTML` sole injection point (unchanged); `createMarkElement` uses textContent/DOM only; notes as React text children — zero XSS surface. IDOR: all PATCH/DELETE `findFirst({where:{id,userId}})` — 404 on miss; GET scoped by session. `OpenSurface` state machine: mutual exclusion correct; no double-open or stuck-surface paths. Migration purely additive, cascades correct. Tests adequate (28 new: validateAnchor + lib CRUD + route 401/404/400/200-201 + IDOR). FIX-BEFORE-LAND (all resolved): F1 `splitText` crash on overlapping DB highlights (Linus: offset clamp guard); F2 `HighlightEditPopover` positioning `useEffect` no-dep-array → per-keystroke layout thrash (Linus: `[anchorEl]`); F3 server note cap 50k vs client 2k (Livingston: `HIGHLIGHT_NOTE_MAX=2_000` both routes). 5 deferrable nits: N1 disabled-logic (`disabled={atLimit}`), N2 `flashAndScroll` template-literal selector, N3 Escape focus-restoration on non-focusable `<mark>`, N4 overlap-merge sequential removes (no rollback), N5 80ms setTimeout fragile on slow renders.

### M12 — AI Tutor Code Review (2026-06-19) ✅ APPROVE-WITH-NITS — committed 96ab8d0
Reviewed M12 (Linus UI + Livingston backend). **APPROVE-WITH-NITS** — no XSS, no IDOR. One FIX-BEFORE-LAND (F1): `@media (prefers-reduced-motion)` CSS rule targeted `.rw-tutor-typing-label` but element class is `.rw-tutor-thinking-label` — a11y regression (thinking state invisible under reduced-motion); fixed by Linus pre-land. Static gates at review: typecheck 0 · lint 0 · build green · 267/267 tests. IDOR audit: `GET/POST/DELETE /api/reader/[id]/tutor` all scope by session userId; `DELETE` clears only session user's messages; no cross-user message access path. XSS audit: `tutor-markdown.ts` tokenizer returns plain token objects (not HTML strings); React renders as text nodes — no `dangerouslySetInnerHTML` path in tutor flow. Deferrable nits: streamed responses (performance), composer max-height, abort-on-navigate for in-flight POST. Migration purely additive, cascades correct. All must-not-break contracts untouched.

### M13 — Sentence-level Translation Code Review (2026-06-19) ✅ APPROVE-WITH-NITS — committed 47f7aa6
Reviewed M13 (Linus UI + Livingston backend). **APPROVE-WITH-NITS** — no fix-before-land. Selection-surface integrity: `"translate"` correctly added to `OpenSurface`; `toolbar→translate` direct transition preserves `savedAnchorRef`; `closeAll` resets translate state retaining `translateLang`; Escape works on all surfaces; no stuck state. Stale-request guard: `translateReqRef` counter correct; language change mid-flight discards old result. Safe rendering: zero `dangerouslySetInnerHTML` in popover; `lang`+`dir="auto"` RTL correct. Backend: cache key `(articleId, SHA-256, lang)` stable; `MAX_SENTENCE_CHARS` shared; cascade DELETE wired; graceful fallback never cached. Shared lang key: `translate-lang.ts` single source of truth; both surfaces read/write correctly. Reader integrity: no regressions; accent rule upheld; a11y correct (`role="dialog"`, `aria-busy`, `aria-live`, focus management). 4 deferrable nits: N1 lang seed validation, N2 client char guard, N3 toolbar order vs spec diagram, N4 redundant stopPropagation.

### M14 — Quiz Mastery & History Review (2026-06-19) ✅ APPROVE-WITH-NITS — committed 01380fc
Reviewed M14 across 5 scopes. Grading unchanged: `selectAnswer`/score memo/`allAnswered`/per-question classes/`correctIndex` verbatim identical to M5. Record-once: `recordedRef` set synchronously before fetch; Check-answers unmounted post-submission (belt-and-suspenders); `handleRetry` resets ref; POST failure isolates to `savedNote="failed"`. IDOR clean: all queries scoped to `session.user.id`; `scorePct` server-derived; body schema rejects client-supplied score. Data correctness: Prisma `_avg` for mean; `recentTrend` desc→reversed to oldest-first; `_max` post-insert. Nit N1: "Try again" should be `btn-primary` (deferrable). Nit N2: unused `active` prop (deferrable). No blockers, no security issues.

### M15 — Personalized home feed Review (2026-06-19) ✅ APPROVE-WITH-NITS — committed e504ef0
Reviewed M15 across 5 scopes: (1) Card DOM contract — all 5 `js-progress-*` hooks + `data-article-id` (wrapper+Link) + `.js-bookmark` untouched; `.rw-why-chip` normal-flow `<span>` between byline ③ and `mt-auto` footer, zero interference. ✅ (2) Feed ranking + endpoint — 4 batched DB queries (no N+1); completed hard-excluded; in-progress −15; no-profile fallback; `createHandler` + `session.user.id` (no IDOR); NOT cached; `hasMore`/`offset` correct. ✅ (3) Dashboard rework — level filter/`listPublishedArticles`/`filterAndSortByLevel` removed cleanly; section order matches spec; rail/feed sync sets disjoint. ✅ (4) Quality — chip palette muted neutral (NOT teal/indigo); a11y: chip `aria-label` + `aria-hidden` icon; `ForYouFeed section aria-labelledby`; load-more `aria-live="polite"`; end-of-feed `role="status"`; globals.css additive only. ✅ (5) Stray files — 3 `_audit_*.ts` flagged (coordinator removed 4); `_audit_session.ts` had hardcoded userId; all removed pre-commit. ✅ No blockers, no security issues. 3 nits deferred.
