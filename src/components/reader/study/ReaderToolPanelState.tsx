"use client";

/**
 * @deprecated Moved to `@/components/ui/ReaderToolPanelState` (promoted to ui/,
 * FE-13). Import the panel-state primitives from `@/components/ui` instead.
 * Re-exported here for backward compatibility.
 */
export {
  PanelLoading,
  PanelError,
  PanelFallback,
  PanelEmpty,
} from "@/components/ui/ReaderToolPanelState";
