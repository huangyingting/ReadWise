/**
 * Re-export shim — flashcard helpers now live in the consolidated learning
 * subsystem (REF-028). Kept for backward compatibility with existing importers;
 * prefer `@/lib/learning/flashcards` for new code.
 */
export * from "@/lib/learning/flashcards";
