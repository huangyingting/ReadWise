/**
 * Semantic / vector search provider placeholder (ARCH-15).
 *
 * A future pgvector or embedding-based provider would be implemented here and
 * registered via `registerSearchProvider` from `@/lib/search/providers`.
 * Until that integration lands, this file is intentionally empty.
 */
