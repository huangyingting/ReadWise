/**
 * Re-export shim — study-plan generation now lives in the consolidated learning
 * subsystem (REF-028). Kept for backward compatibility with existing importers;
 * prefer `@/lib/learning/study-plan` for new code.
 */
export * from "@/lib/learning/study-plan";
