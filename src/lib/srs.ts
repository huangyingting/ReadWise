/**
 * Re-export shim — SM-2 SRS engine now lives in the consolidated learning
 * subsystem (REF-028). Kept for backward compatibility with existing importers;
 * prefer `@/lib/learning/srs` for new code.
 */
export * from "@/lib/learning/srs";
