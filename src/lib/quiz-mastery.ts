/**
 * Re-export shim — quiz mastery now lives in the consolidated learning
 * subsystem (REF-028). Kept for backward compatibility with existing importers;
 * prefer `@/lib/learning/quiz-mastery` for new code.
 */
export * from "@/lib/learning/quiz-mastery";
