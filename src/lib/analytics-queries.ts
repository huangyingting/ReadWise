/**
 * Compatibility shim — all exports have moved to the analytics query subsystem.
 * @deprecated Import from `@/lib/analytics/queries` (or its sub-modules) directly.
 */
export * from "@/lib/analytics/queries";
