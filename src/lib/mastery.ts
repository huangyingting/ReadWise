/**
 * Re-export shim — shared learning-mastery primitives now live in the
 * consolidated learning subsystem (REF-028). Kept for backward compatibility
 * with existing importers; prefer `@/lib/learning/primitives` for new code.
 */
export * from "@/lib/learning/primitives";
